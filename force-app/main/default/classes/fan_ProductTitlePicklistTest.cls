@isTest
public with sharing class fan_ProductTitlePicklistTest {
    @IsTest
    static void getValues(){
        fan_ProductTitlePicklist test = new fan_ProductTitlePicklist();
        test.getValues();
    }

    @IsTest
    static void getDefaultValue(){
        fan_ProductTitlePicklist test = new fan_ProductTitlePicklist();
        test.getDefaultValue();
    }
}